---
name: 'bmad-bmm-qa-automate'
description: 'Generate automated API and E2E tests for implemented code using the projects existing test framework (detects existing well known in use test frameworks). Use after implementation to add test coverage. NOT for code review or story validation - use CR for that.'
---

Run the BMAD skill `bmad-qa-generate-e2e-tests` immediately.

INSTRUCTIONS:

1. LOAD the skill file from {project-root}/.opencode/skills/bmad-qa-generate-e2e-tests/SKILL.md
2. READ its entire contents
3. FOLLOW every instruction in that skill exactly
4. Stay in the selected BMAD workflow/agent until completion or explicit exit
