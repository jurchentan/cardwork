---
name: 'bmad-distillator'
description: 'Lossless LLM-optimized compression of source documents. Use when you need token-efficient distillates that preserve all information for downstream LLM consumption.'
---

Run the BMAD skill `bmad-distillator` immediately.

INSTRUCTIONS:

1. LOAD the skill file from {project-root}/.opencode/skills/bmad-distillator/SKILL.md
2. READ its entire contents
3. FOLLOW every instruction in that skill exactly
4. Stay in the selected BMAD workflow/agent until completion or explicit exit
